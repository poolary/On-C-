#include <iostream> // o comando include serve para:fzr inclusão de bibliotecas. Saber onde está o comando e tal. A "iostream" é a biblioteca e estou usando, io é de entrada e saída, in e out

using namespace std; //standar o nome do std, outro namespace, using é de usando, namespace é q vou indicar onde estão as bibliotecas q vamos usar.

int main(){  // a funcionalidade do programa vai estar descrito no corpo da funçõo main, eai eu informo o tipo de dado q vai retornar. Int de inteiro. Ou poderia usar o void, que não volta funcionalidade

    cout << "Hello world" //c out, ou cout, serve para imprimir oq quero escrever

     return 0; // é dessa forma básica que preciso para criar algo. OBS: Eu clico o F9 para compilar, com ele vejo se tem erro e tal.
}
